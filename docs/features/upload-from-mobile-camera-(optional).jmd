{% extends "_templates/base.html" %}
{% block content %}
{% markdown %}

# Upload from Mobile Device <small>(optional)</small> {: .page-header }

{{ options_table(
    (
        (),
    )
) }}

{{ events_table(
    (
        (),
    )
) }}

{{ methods_table(
    (
        (),
    )
) }}

{% endmarkdown %}
{% endblock %}