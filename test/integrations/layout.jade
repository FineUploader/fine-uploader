!!! 5
html
  head
    title Fine Uploader
    meta(http-equiv='Content-Type', content='text/html; charset=UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')

    // mocha
    link(href='/vendor/mocha.css', rel='stylesheet', type='text/css')
    script(src='/vendor/mocha.js', type='text/javascript')

    //chai
    script(src='/vendor/chai.js', type='text/javascript')
    script
        mocha.setup('bdd');
        mocha.checkLeaks();

        assert = chai.assert;
        expects = chai.expect;

        var errorHandler = function (event, id, filename, reason) {
            qq.log("id: " + id + ", filname: " + filename + ", reason: " + reason); 
        };


    //jQuery
    script(src='/vendor/jquery-1.10.0.min.js')

    //Fine Uploader sources
    link(href="/fine-uploader/css/fine-uploader.css")
    script(src="/fine-uploader/js/fine-uploader.js")

  body
    h1 Fine Uploader Integration Tests 

    #test-info
        block test-information

    hr
            
    #fixture

    #mocha

    block test-runners

